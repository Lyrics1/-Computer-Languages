#include<stdio.h>
#include<stdlib.h>
#include "¶þ²æÊ÷.h"

int depth;
int main()
 	{
 		Bitree root;
 		creat(&root);	
 		preorder(root);
 		printf("\n");
 		printf("%d\n\n",Two);
 		output(root);
 		printf("\n");
 		tree(root,1);
 		change(root);
 		preorder(root);
 		printf("\n");
		 depth=treedepth(root);
		char ph[depth];
		path(root,ph,depth);
		print(root,1);
		int leaf=leafnum(root);
		printf("%d",leaf);
 		
	 }
 	
typedef char Date;
 
typedef struct Nodestack
{
	Bitree a[depth];

 } *StackTree;
 
  
